export const dynamic = 'force-dynamic'

export default function TestimonialLayout({
    children,
}: Readonly<{
    children: React.ReactNode;
}>) {
    return (
        <>
            {children}
        </>
    );
}
